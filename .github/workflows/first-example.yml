name: first workflow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: welcome message
              run: echo "welcome"
            
            - name: list files
              run: ls

            - name: read file
              run: cat READMe.md